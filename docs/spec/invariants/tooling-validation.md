# Validation, CI, packaging, docs, and governance invariants

Authority: registered by `docs/spec/registry.json`; IDs remain stable and semantics are copied verbatim from the former monolithic §V.

| id | invariant | source |
|----|-----------|--------|
| V22 | Sharded unit runner: per-shard JSON summary + recoverable logs; default `gui` shard, opt-in `hardware` shard separate | tests/unit/test_ci_sharded_unit_validation.py, docs/VALIDATION_SHARDS.md |
| V130 | Default pytest gate ! plain `uv run pytest` skips slow/integration/GPU/GUI/hardware tiers unless explicit `--run-*` opt-in is passed; hardware-facing checks stay hidden from normal full/local runs. Sharded runner must inject matching opt-ins for optional shards. Default quick gate target ≤8min on WSL2 dev shell | tests/conftest.py; tests/unit/test_ci_sharded_unit_validation.py; docs/VALIDATION_SHARDS.md; B59 |
| V637 | Pytest coverage gate precision ! Default `uv run pytest` coverage gate reports and fails under same two-decimal total; `86.72% < 87.00%` fails, `87.00%` passes; threshold must not rely on integer rounding | pyproject.toml; tests/unit/test_coverage_gate_precision.py; B534 |
| V638 | Pure Nix default profile contract ! Default `complex64-cuda` dev shell uses Nix-provided Python/runtime deps (`PYEIDORS_ACTIVE_ENV=nix`, `VIRTUAL_ENV` unset, repo `src/` on `PYTHONPATH`) and must not auto-create/sync `.venv*`; manifest verification resolves profile-specific locks such as `env/manifests/linux-x86_64-complex64-cuda.lock.json` and compares Nix lock/profile/package facts, not legacy uv sync flags | flake.nix; scripts/env/export_env_manifest.py; scripts/env/verify_env_manifest.py; tests/integration/test_env_repro_mac_linux_contract.py; B535 |
| V649 | Sharded pytest Nix runner ! sharded validation dry-run/run commands use `nix develop .#complex64-cuda -c python -m pytest ...`; `uv run` remains explicit legacy runner only, never emitted by default Nix shard commands | scripts/ci/run_sharded_unit_tests.py; tests/unit/test_ci_sharded_unit_validation.py; B547 |
| V672 | Pure Nix backend worker package import check ! include `eit_app.ecd_cwr_simulation`; `nix run .#eit-backend-worker-*-cpu -- ecd-cwr-simulate-cem` ⊥ fail from missing packaged ECD-CWR module | flake.nix; tests/unit/test_backend_manifest_doctor.py; B570 |
| V680 | Pure Nix backend wrapper init commands ! resolve from closure; `mkdir`/basic setup uses bundled `coreutils`; `nix run` worker/doctor ⊥ depend on host `/usr/bin` before Python entry | flake.nix; tests/unit/test_backend_manifest_doctor.py; B573 |
| V691 | External author reference snapshots ! untouched/untracked `CEM-via-Robin-Boundary/` excluded from project format mutation/gate; Ruff applies project-owned Python only; external notebook format drift ≠ project failure | CEM-via-Robin-Boundary/; Ruff verification command |
| V766 | Professor-facing PyEIDORS notebooks ! real-`float64` `.#default` Nix profile provides `ipykernel` + JupyterLab; VS Code uses a named kernelspec whose launcher re-enters that profile, ⊥ selecting raw `/nix/store/.../bin/python` without the shell environment | examples/cem_exact_extension_walkthrough/register_vscode_kernel.py; tests/unit/test_cem_exact_extension_walkthrough.py |
| V773 | Advertised `tests/run_all_tests.py` ! invoke pytest on existing `tests/unit/` targets; missing relative path or direct execution of pytest modules ⊥ report false pass/missing | tests/run_all_tests.py; tests/unit/test_run_all_tests.py; FILE_ORGANIZATION.md |
| V777 | SPEC registry integrity ! root `SPEC.md` indexes every registered invariant/history file; every `V/T/B` ID unique repo-wide; every cited ID resolves; §T status ∈ `{x,~, .}`; `docs/spec/id-map.tsv` exactly maps ID→authority file; CI gate scans root + all registered files | scripts/ci/spec_integrity_guard.py; tests/unit/test_spec_integrity_guard.py; docs/spec/registry.json |
| V779 | Refactor/artifact audit ! oversized-module decomposition requires GitNexus impact + preserved §V gates before edits; tracked `reports/`/`pictures/` assets require explicit keep/drop rationale + live consumer/provenance inventory; audit alone ⊥ mutate solver/GUI behavior or delete assets | docs/code-fusion/T615_large_module_and_artifact_audit.md; V75 |
