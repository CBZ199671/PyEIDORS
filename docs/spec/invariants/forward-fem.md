# Forward FEM, CEM, PETSc, and linear solves invariants

Authority: registered by `docs/spec/registry.json`; IDs remain stable and semantics are copied verbatim from the former monolithic §V.

| id | invariant | source |
|----|-----------|--------|
| V1 | CEM forward: single matrix assembly + single `ksp.setUp()` per `sigma`; all RHS share KSP bundle | tests/unit/test_forward_petsc_multirhs.py, test_forward_mat_solve_policy.py |
| V2 | `matSolve` auto-mode: true iff `mesh_tdim==3 AND n_patterns>1 AND performance_mode=="aggressive" AND (min_patterns==0 OR n_patterns>=min_patterns)` | tests/unit/test_forward_mat_solve_policy.py |
| V4 | `backend_signature_from_forward_model` includes `solver_preset`, `pc_factor_mat_solver_type`, `pc_hypre_type`, `pc_gamg_type`, `petsc_options`, `petsc_device_effective`, `mat_solve_mode` | src/pyeidors/cache/object_signature.py |
| V5 | Unknown `solver_preset` → `ValueError` listing sorted preset names | tests/unit/test_forward_solver_presets.py |
| V6 | Auto preset: `mesh_tdim<3` → `direct` (`preonly+lu`); `mesh_tdim>=3` → `3d_gamg` (`fgmres+gamg+agg`); explicit `ksp_type`/`pc_type` → `custom` preset | src/pyeidors/forward/eit_forward_model.py `_resolve_linear_backend_config` |
| V13 | Forward KSP session reused across `_solve_with_petsc` calls when structural fingerprint stable; `ksp.setOperators(A_new)` + `setReusePreconditioner(True)` skips `PCSetUp` | tests/unit/test_forward_ksp_session_reuse.py |
| V14 | PC refresh under `forward_pc_refresh_policy="auto"` triggers when last iter count > `forward_pc_refresh_iter_threshold`; `lag` rebuilds every `forward_pc_refresh_lag` calls; `never` always reuses; structural change forces rebuild | tests/unit/test_forward_ksp_session_reuse.py |
| V16 | `build_process_forward_setup_key(*, mesh_file, mesh_content_hash, …)` requires at least one non-empty of `mesh_file`/`mesh_content_hash`; both empty → `ValueError` | tests/unit/test_forward_process_setup_cache.py |
| V17 | `_hash_mesh_content(dolfinx_mesh)` content-addresses `geometry.x` + `topology.connectivity(tdim,0).array`; returns `""` when mesh lacks geometry/topology (unit-fake permissive) | tests/unit/test_forward_process_setup_cache.py |
| V18 | MPI `comm.size > 1` raises `RuntimeError` at `EITForwardModel.__init__`; single-rank-only phase, T2 unlocks distributed Mat/Vec + `mpiexec -n 2` smoke | src/pyeidors/forward/eit_forward_model.py `_assert_supported_mpi_runtime`, src/pyeidors/perf/capabilities.py `probe_mpi_runtime` |
| V23 | Forward `KSPSetReusePreconditioner(True)` semantics: same KSP, new `setOperators(A_new)`, reuse holds until explicit refresh; iter-count-monitored | src/pyeidors/forward/eit_forward_model.py `ForwardKSPSession`, PETSc `KSPSetReusePreconditioner` manpage |
| V24 | Direct PC (`ksp_type=="preonly"` AND `pc_type ∈ {lu, cholesky, qr}`) never reused across sigma — session forces PC refresh with `forward_pc_refresh_reason="direct_factor_requires_rebuild"`. `preonly` has no Krylov iteration to correct a stale exact factor, unlike iterative+AMG where reuse is a staleness penalty | src/pyeidors/forward/eit_forward_model.py `_decide_pc_reuse_for_session` |
| V43 | AmgX experiment gate: `nix develop .#cuda-amgx` ! real-double CUDA Mat/Vec/Dense + `PETSc.PC.Type.AMGX` + `JACOBI_L1` smoke pass; `nix develop .#complex-cuda-amgx` ! native complex128 PCAMGX smoke pass only with `AGGREGATION/SIZE_8/BLOCK_JACOBI/NOSOLVER`; `benchmark_3d_runtime.py --forward-only on --forward-solver-preset cuda_amgx --petsc-device cuda --forward-mat-solve off` completes, reports `ksp_type=fgmres`, `pc_type=amgx`, `forward_amg_backend=amgx`, speed/residual vs `3d_gamg + petsc_device=cuda`, and no `cuda_dense_lu_fallback:*` bypass. Plain FEniCSx/DOLFINx upgrade does not satisfy this gate | `.#cuda-amgx`/`.#complex-cuda-amgx` profiles + scripts/diagnostics/probe_petsc_cuda.py; tests/unit/test_forward_solver_presets.py; tests/unit/test_script_entrypoint_acceleration_profiles.py |
| V47 | Current 3D forward default: PETSc sparse `spd_gamg + petsc_device=cuda + forward_mat_solve=off`; generic direct LU allowed for 2D/tiny-3D/debug only; MUMPS preset retired after 3D complex CEM mismatch vs CPU SciPy direct / CUDA dense LU; Hypre CUDA blacklisted; AmgX research gated by V43 | V6,V42,V43,V44,V45; reports/benchmarks/forward_spd_gamg_cuda_48e_repeat2_20260421.json |
| V66 | Mesh first-load policy: if XDMF/HDF5 cache exists, `MeshLoader` loads it first even when source `.msh` missing; metadata/freshness may use explicit mesh content hash or cache manifest, not mandatory `.msh` mtime. If only `.msh` exists, import once → write XDMF/HDF5 → subsequent loads never parse `.msh`. Mesh geometry, cell tags, facet tags, physical group association, structured sidecar fields must round-trip | src/pyeidors/geometry/mesh_loader.py; src/pyeidors/geometry/dolfinx_mesh_cache.py; local benchmark 2026-04-22 |
| V76 | Semantic cache signatures ! content-safe. `model_signature_from_forward_model` / `pattern_signature_from_forward_model` / `backend_signature_from_forward_model` 必随 math-affecting content change而变；⊥ 裸 `id(obj)` memoization 覆盖 mutable `stim_matrix` / `meas_matrices` / `backend_config` / `_petsc_backend_info` 原地修改. 允许 memoize iff key 含 content hash / immutable generation token / explicit version bump; tests 必含 in-place mutation gate | tests/unit/test_cache_semantic_signature.py; src/pyeidors/cache/object_signature.py; T88,T90 |
| V85 | PETSc options database hygiene: project-scoped KSP options injected via `pyeidors_forward_*_` prefix ! cleaned after `setFromOptions()` success/failure; auto-added `pc_hypre_type`/`pc_gamg_type`/factor opts only when active PC type can consume them. Full unit gate ⊥ PETSc unused database warning from project-injected forward options | tests/unit/test_forward_petsc_helper_branches.py; B23 |
| V111 | CUDA CEM same-mesh forward parity ! PETSc CUDA CEM route uses equivalent reference-electrode gauge: replace only global constraint row with \(U_0=0\), keep every electrode balance equation and Lagrange column, then recenter electrode voltages to zero mean. CUDA iterative CEM routes that are neither direct LU/Cholesky/QR nor explicit PCAMGX smoke-passed route must fall back to CUDA dense direct solve before returning visual/forward data. Exact EIDORS stim/meas replay requires `corr(ΔV)>0.99` | tests/unit/test_forward_petsc_helper_branches.py; tests/unit/test_forward_mat_solve_policy.py; tests/integration/test_eidors_forward_parity_gate.py; scripts/diagnostics/eidors_forward_parity_gate.py; B45,B560 |
| V131 | Complex CUDA PETSc profiles ! `complex-cuda`/`complex64-cuda` build PETSc via scalar/precision override, and complex PETSc build disables real-scalar external solver packages Hypre/SuperLU_DIST/FFTW/SuiteSparse. GPU complex CEM route uses DOLFINx/PETSc CUDA, ⊥ `cuda_structured` | tests/unit/test_flake_complex_petsc_profiles.py; B60 |
| V144 | Generated mesh cache dtype ! auto-generated mesh cache names, process cache keys, and DOLFINx XDMF metadata include mesh geometry dtype. `complex64`/single-precision runtimes must use `Mesh_float32` with `Form_complex64`; they must not reuse legacy/default `Mesh_float64` caches. Default `float64` cache names stay byte-stable for existing real/complex128 caches | tests/unit/test_mesh_helpers_consolidation.py; tests/unit/test_optimized_mesh_generator_branch_paths.py; tests/unit/test_dolfinx_mesh_cache.py; B75 |
| V153 | `pyeidors.geometry` import + mesh generator module imports ! lazy-light. Importing package declares `__all__` but ⊥ imports mesh generator/converter/derived-cache modules. Importing generator/converter modules may inspect `gmsh` availability but ⊥ loads `gmsh`, `meshio`, `mpi4py`, or DOLFINx IO until mesh IO/generation call | src/pyeidors/geometry/__init__.py; src/pyeidors/geometry/_runtime.py; tests/unit/test_mesh_generator_lazy_gmsh.py |
| V154 | Top-level `import pyeidors` ! lazy-light. Import declares façade `__all__`, `check_environment`, `__version__`, `EITSystem` lazy attr, and compatibility env flags in `dir()`; ⊥ imports DOLFINx/Torch/CUQI/core_system. `check_environment()` / private `_DOLFINX_AVAILABLE` etc. perform cached runtime probes on first use | src/pyeidors/__init__.py; tests/unit/test_public_api_and_launcher_contract.py; tests/unit/test_pyeidors.py |
| V157 | `pyeidors.forward` package import ! lazy-light. Import declares CEM/backend/scalar helper exports but ⊥ imports DOLFINx/PETSc/Torch/meshio or forward implementation modules. Public symbols resolve via first attr/import and cache into package globals; `eit-cache` import-health includes forward package + PETSc/forward-heavy sentinels | src/pyeidors/forward/__init__.py; src/pyeidors/cache/ops.py; tests/unit/test_public_api_and_launcher_contract.py; tests/unit/test_cache_ops_cli.py |
| V158 | `pyeidors.forward.complex_support` module import ! PETSc-light. Importing scalar helper module may load NumPy but ⊥ imports `petsc4py`; first scalar query lazily resolves/caches PETSc and preserves monkeypatchable `PETSc` attr for tests/external probes | src/pyeidors/forward/complex_support.py; tests/unit/test_public_api_and_launcher_contract.py; tests/unit/test_forward_complex_admittivity.py |
| V161 | `pyeidors.visualization` package import ! lazy-light. Import declares `EITVisualizer` / `create_visualizer` but ⊥ imports Matplotlib, DOLFINx, UFL, MPI, or visualization implementation modules until plotting symbol access. `eit-cache` import-health tracks visualization-heavy sentinels | src/pyeidors/visualization/__init__.py; src/pyeidors/cache/ops.py; tests/unit/test_public_api_and_launcher_contract.py |
| V170 | `pyeidors.femx` package import ! lazy-light. Import declares FEM helper exports but ⊥ imports DOLFINx, UFL, or `pyeidors.femx.helpers` until helper/submodule access. `dir()` still exposes `helpers` submodule | src/pyeidors/femx/__init__.py; src/pyeidors/cache/ops.py; tests/unit/test_public_api_and_launcher_contract.py |
| V238 | CUDA structured Jacobi preconditioner budget ! `_build_sigma_state` computes diagonal inverse on target torch device via `torch.reciprocal(...).reshape(-1,1)` and must not materialize NumPy `(1.0 / diag)[:, None]` before GPU transfer; PCG solve semantics unchanged | src/pyeidors/forward/cuda_structured_backend.py; tests/unit/test_cuda_structured_backend.py |
| V258 | Forward-model cache hash streaming ! `_hash_mesh_content` and `_hash_scalar_array` preserve legacy mesh/scalar digest bytes while streaming ndarray payload updates through `update_digest_with_array_payload`; forward mesh/sigma/z/pattern cache hash helpers must not locally `.tobytes()` large arrays | src/pyeidors/forward/eit_forward_model.py; tests/unit/test_forward_process_setup_cache.py |
| V272 | 3D diagnostic/benchmark phantom distance budget ! 3D runtime, CEM cylinder smoke, reconstruction overview/gallery, and p-refinement scripts use `squared_distances_to_point` for anomaly/ROI masks and must not allocate `coords - center[None,:]` matrices; mask membership remains unchanged | scripts/benchmarks/benchmark_3d_runtime.py; scripts/run_cem_16e_cylinder_3d_test.py; scripts/diagnostics/render_3d_inverse_reconstruction_overview.py; scripts/diagnostics/gallery_shared.py; scripts/diagnostics/run_real_reconstruction_gallery_worker.py; scripts/benchmarks/benchmark_p_refinement_forward.py; tests/unit/test_script_hash_streaming.py |
| V397 | Mesh-derived cold-build single extraction budget ! `build_mesh_derived_arrays()` and `load_or_build_mesh_derived_artifact()` reuse one extracted `(coords,cells,tdim)` bundle for centers, measures, signature payload, and metadata; cold artifact build must not re-run `_mesh_cells()` / DOLFINx connectivity traversal solely to compute signature after deriving arrays | src/pyeidors/geometry/derived_cache.py; tests/unit/test_mesh_derived_cache.py; docs/CACHE_ARCHITECTURE.md |
| V417 | PETSc electrode coupling nonzero budget ! `_assemble_electrode_matrix_petsc()` obtains non-zero coupling column indices and values via `_nonzero_index_value_arrays()` direct fill and must not allocate `np.flatnonzero(c_i)` or `c_i[nz]` advanced-index value copies per electrode | src/pyeidors/forward/eit_forward_model.py; tests/unit/test_forward_solver_branch_suite.py; docs/CACHE_ARCHITECTURE.md |
| V476 | Forward finite-scan budget ! `pyeidors.utils.numeric_ops.all_finite_values()` provides bounded reusable finite checks; CEM scalar coercion and CUDA structured diagonal validation must use it instead of full `np.isfinite(...).all()` bool payloads. Non-finite errors and invalid-diagonal semantics stay unchanged | src/pyeidors/utils/numeric_ops.py; src/pyeidors/forward/eit_forward_model.py; src/pyeidors/forward/cuda_structured_backend.py; tests/unit/test_numeric_ops_warning_free.py; tests/unit/test_forward_complex_admittivity.py; tests/unit/test_cuda_structured_backend.py; docs/CACHE_ARCHITECTURE.md |
| V592 | Mesh-derived cell-connectivity extraction startup budget ! `_mesh_cells()` uses DOLFINx-style flat `connectivity.array` + `offsets` when available to build the local cell matrix in one reshape instead of calling `connectivity.links(i)` per cell. Irregular/legacy connectivity still falls back to links, dtype normalization stays int32, and derived HDF5 signature/centers/measures remain unchanged | src/pyeidors/geometry/derived_cache.py; tests/unit/test_mesh_derived_cache.py; docs/CACHE_ARCHITECTURE.md |
| V593 | CUDA structured sigma-hash noncontiguous budget ! `CudaStructuredForwardBackend._stable_hash()` keeps already-float64 sigma inputs as ndarray views and delegates C-order byte streaming to the shared cache-key helper instead of materializing a full contiguous staging copy. Float64 hash bytes, dtype-cast behavior for non-float64 inputs, and sigma-state reuse semantics stay unchanged | src/pyeidors/forward/cuda_structured_backend.py; tests/unit/test_cuda_structured_backend.py; docs/CACHE_ARCHITECTURE.md |
| V607 | Process forward-setup cache byte-budget LRU ! `ForwardStaticSetupBundle` process cache uses resident-byte accounting with `PYEIDORS_PROCESS_FORWARD_SETUP_CACHE_MAX_BYTES` / `EIT_APP_PROCESS_FORWARD_SETUP_CACHE_MAX_BYTES` (default 512MiB). Electrode length arrays and CEM electrode CSR matrix payloads count toward retention; oversize setup bundles are not retained and same-key oversize writes evict stale entries. Forward setup key bytes, scalar/profile separation, and static setup reuse semantics stay unchanged | src/pyeidors/forward/process_setup_cache.py; src/pyeidors/cache/process_lru.py; tests/unit/test_process_lru_consolidation.py; docs/CACHE_ARCHITECTURE.md |
| V633 | PETSc/CUDA CEM forward gauge parity ! `_solve_with_petsc` returns electrode voltages in the same zero-mean gauge as SciPy for all solve routes (`matSolve`, dense fallback, vec-loop); backend comparisons may use complex64 tolerances, but measurement projection must agree after gauge normalization | src/pyeidors/forward/eit_forward_model.py; tests/unit/test_forward_vectorized_runtime.py; B530 |
| V642 | Nix subprocess import tests ! isolated Python subprocesses that add repo `src/` preserve existing Nix `PYTHONPATH` entries; bare `PYTHONPATH=src` must not drop NumPy/SciPy/FEniCSx site-packages from Nix dev shell | tests/unit/test_public_api_and_launcher_contract.py; B540 |
| V643 | Complex PETSc FEM scalar bridge ! DOLFINx `fem.Constant`/Form scalars use active PETSc scalar dtype; real diagnostics convert zero-imag assemble/allreduce/sigma arrays via checked bridge, never implicit `float()`/`astype(float)` in complex64-cuda | src/pyeidors/geometry/optimized_mesh_generator.py; src/pyeidors/visualization/eit_plot_helpers.py; scripts/run_cem_16e_square_test.py; scripts/run_cem_16e_cylinder_3d_test.py; tests/integration/test_run_cem_inverse_strict.py; B541 |
| V646 | Complex64 parity gates ! fast/strict and unit-scale integration tolerances follow active PETSc scalar precision; complex64/float32 use single-precision bounds, complex128/float64 keep double bound; `native-complex` valid fast path | tests/integration/test_3d_absolute_fast_vs_strict.py; tests/integration/test_unit_scale_invariance_mm_cm_m.py; B544 |
| V647 | Lazy FEM/ufl complex test fakes ! unit tests that exercise optimized mesh/visualization FEM-form branches install module-level fake `fem`/`ufl` objects and route zero-imag PETSc scalar constants through checked bridge; tests do not depend on prior `_ensure_femx()` side effects or `float(complex)` casts | tests/unit/test_forward_and_mesh_helper_edges.py; tests/unit/test_visualization_helper_extra_edges.py; B545 |
| V648 | GitNexus Node in Nix shells ! pure Nix dev shells provide Nix `nodejs` on PATH before system `/usr/bin/node`, so GitNexus CLI/MCP analyze commands do not mix Ubuntu Node with Nix `libstdc++`/glibc ABI paths | flake.nix; tests/unit/test_flake_complex_petsc_profiles.py; B546 |
| V660 | PETSc solver fallback resource hygiene ! every failed temporary KSP/PC candidate created while building a forward solver bundle is explicitly destroyed before trying the next candidate or gmres fallback; dense fallback Mat copies created only for a failed attempt are destroyed too, so AmgX/PC resources never rely on Python finalizers after setup failure | src/pyeidors/forward/eit_forward_model.py; tests/unit/test_forward_solver_branch_suite.py; B559 |
| V661 | Explicit PCAMGX CEM route ! `solver_preset ∈ {amgx,cuda_amgx,complex_cuda_amgx}` or `pc_type=amgx` + `petsc_device_effective=cuda` + `petsc_amgx_cuda_candidate=true` keeps CEM reference-electrode gauge on sparse CUDA KSP, uses `fgmres+amgx`; real route stable opts `JACOBI_L1/exact=0/presweeps=2/postsweeps=2/coarse=NOSOLVER`; native complex route stable opts `AGGREGATION/SIZE_8/BLOCK_JACOBI/exact=0/presweeps=2/postsweeps=2/coarse=NOSOLVER`; sets `cuda_cem_direct_fallback_suppressed=pcamgx_explicit` instead of `cuda_dense_lu_fallback:*`, and refuses `gmres+none` fallback if PCAMGX setup fails | src/pyeidors/forward/eit_forward_model.py; tests/unit/test_forward_mat_solve_policy.py; tests/unit/test_forward_solver_presets.py; tests/unit/test_forward_solver_branch_suite.py; B560 |
| V662 | Explicit PCAMGX solve failure ! after PCAMGX setup succeeds, negative KSP convergence in forward or full-RHS solve sets `explicit_pcamgx_solve_failed_refused_fallback:<reason>` and raises; ⊥ CUDA dense direct fallback, ⊥ CPU fallback, ⊥ successful benchmark/GUI result that hides failed AmgX solve | src/pyeidors/forward/eit_forward_model.py; tests/unit/test_forward_solver_branch_suite.py; B561 |
| V664 | Complex block-real AmgX parity ! exported complex CEM matrix/RHS uses independent SciPy sparse LU direct reference as truth; block-real PCAMGX candidate compared after matching reference-electrode/zero-mean gauge; fair reports include full-solution and electrode-voltage relL2 gates; default block-real AmgX profile uses setup-proven `real_jacobi_l1`, while `block_jacobi` remains explicit experiment | src/pyeidors/utils/block_real.py; scripts/diagnostics/complex_block_real_amgx_probe.py; scripts/diagnostics/complex_block_real_amgx_fair_compare.py; tests/unit/test_block_real_utils.py; tests/unit/test_complex_block_real_amgx_probe.py; tests/unit/test_complex_block_real_amgx_fair_compare.py; B563 |
| V683 | Robin-transconductance CEM ! voltage-driven local Robin block `A_R=K+B`; deterministic zero-sum orthonormal `Q`; reduced map `T_r=Q.T@(D-C.T@A_R^-1@C)@Q`; solve `T_r y=Q.T I`, `U=Qy`, `u=-A_R^-1 C Q y`; full singular `T` ⊥ direct invert; complex reciprocal form uses nonconjugate transpose | src/pyeidors/forward/robin_transconductance.py; tests/unit/test_robin_transconductance_cem.py |
| V685 | Robin runtime ! ∀ basis RHS share one `A_R` assembly + one KSP setup per σ; state cache isolated by σ/scalar/backend; diagnostics record formulation/rank/condition/residual/cache/fallback; imbalanced current, rank loss, nonfinite/ill-conditioned reduced map → explicit error, ⊥ pseudoinverse concealment | src/pyeidors/forward/robin_transconductance.py; tests/unit/test_robin_transconductance_cem.py |
| V690 | Iterative/direct parity tolerance ! assertion bound ≥ requested KSP `rtol/atol` translated to observed true residual + scalar epsilon floor; ⊥ float64 machine-precision threshold for intentionally inexact GMRES; direct-LU CEM equivalence remains epsilon-scale | tests/unit/test_robin_transconductance_cem.py; src/pyeidors/forward/robin_transconductance.py |
| V692 | Full-suite harness ! outer command timeout ≥ known suite envelope (10 min here) or progress-preserving monitor; exit 124 + post-kill PETSc broken-pipe/MPI abort classified harness timeout, ⊥ test assertion failure | full pytest verification command |
| V693 | CEM absolute truth independence ! canonical nodes/cells/tagged edges + exact-promoted float64 coordinates/currents → P1 triangle stiffness & electrode boundary mass/coupling/diagonal assembled analytically in `mpmath`; reference ⊥ consume PyEIDORS/PETSc, NGSolve, EIDORS, SciPy, MATLAB assembled matrices | scripts/benchmarks/cem_high_precision_reference.py; tests/unit/test_cem_high_precision_reference.py |
| V694 | Multiprecision reference certification ! solve identical classic augmented zero-mean CEM @ 80 dps & 128 dps; reference voltage convergence relative L2 ≤`1e-60` + high-precision scaled residual ≤`1e-70`; failure → no solver ranking | scripts/benchmarks/cem_high_precision_reference.py; tests/unit/test_cem_high_precision_reference.py |
| V702 | Discrete mathematical truth ! exact `QQ` analytic P1 CEM assembly + augmented zero-mean solve + rational Robin Schur solve; exact residual numerators all zero & classic/Robin exact `U` identical; solver matrix reuse or floating truth fallback ⊥; singular/nonexact case → no ranking | scripts/benchmarks/cem_exact_reference_suite.py; tests/unit/test_cem_exact_reference_suite.py |
| V706 | Professor-facing CEM exact-accuracy report ! technical summary→visual findings→scope/metric definitions→exact arithmetic proof→experiment design→robustness/limits→next steps; every quantitative claim traceable to suite CSV/JSON; charts Times New Roman & final report render validated | docs/benchmarks/cem_exact_accuracy_report.md; CEM exact-accuracy MCP report |
| V707 | Exact-suite connectivity index contract ! in-memory nodes/cells/edges/electrode nodes all 0-based; MAT `elems/boundary_edges/electrode_nodes` all 1-based; saved MAT electrode rows equal tagged electrode boundary rows and DOLFINx import recovers exactly one positive-measure facet per electrode; mixed-base serialization ⊥ | scripts/benchmarks/cem_exact_reference_suite.py; tests/unit/test_cem_exact_reference_suite.py |
| V710 | True-circle CEM h-sequence ! fixed unit-disk CAD + 16 circular-arc electrodes, coverage=`0.7`, identical physical endpoints across levels; target `h/R∈{1/4,1/8,1/16,1/32}`; boundary chord + interior `h_max` decrease strictly; boundary nodes lie on `R=1` within float64 tolerance; each level exports one canonical P1 MAT/MSH/fingerprint imported unchanged by PyEIDORS/NGSolve/EIDORS | scripts/benchmarks/cem_continuum_reference_suite.py; tests/unit/test_cem_continuum_reference.py |
| V711 | Independent continuum CEM reference ! homogeneous disk analytic Neumann-to-Dirichlet multiplier `R/(sigma*abs(n))` for `n≠0`; electrode `u+z*q=U_l`, gap `q=0`, `∫E_l q ds=I_l`, `sum(U)=0`; reference ⊥ consume interior FEM mesh or any PyEIDORS/NGSolve/EIDORS assembled matrix | scripts/benchmarks/cem_continuum_reference.py; tests/unit/test_cem_continuum_reference.py |
| V715 | Professor-facing continuum CEM report ! explain continuous PDE/NtD derivation, reference independence/certification, true-circle mesh construction, controlled P1 fairness, convergence figures/tables, uncertainty-aware conclusion & limitations; every number traceable to strict finite CSV/JSON; plots use Times New Roman; exact-discrete report remains separate | docs/benchmarks/cem_continuum_accuracy_report.md; scripts/benchmarks/cem_continuum_reference_suite.py; tests/unit/test_cem_continuum_reference.py |
| V768 | Professor-facing CEM evidence ! every framework walkthrough defines each path/matrix/state variable with physical meaning+dimension and renders the same canonical mesh, per-cell conductivity, electrode labels, selected boundary drive, P1/dtype/fingerprint; companion exact-truth Notebook rebuilds X01 from rational inputs, solves Classic+Robin over `QQ`, proves exact full rank/zero residual/gauge/identity/hash, reproduces `float64` forward error+scaled residual, and states discrete truth ≠ continuum analytic truth | examples/cem_exact_extension_walkthrough/build_notebooks.py; examples/cem_exact_extension_walkthrough/experiment_common.py; examples/cem_exact_extension_walkthrough/eidors_selected_case_walkthrough.m; examples/cem_exact_extension_walkthrough/validate_walkthrough.py; tests/unit/test_cem_exact_extension_walkthrough.py |
| V771 | Pure-Nix install/runtime host-env isolation ! 外层解包与内层安装在调用 host tool/Nix 前清除 `LD_PRELOAD`、`PYTHONHOME/PYTHONPATH/PYTHONSTARTUP/PYTHONUSERBASE/VIRTUAL_ENV/CONDA_*` 与 host CUDA/toolchain override；launcher同样清除，Nix wrapper强制自身 `CUDA_HOME/CUDA_PATH/CUDACXX/PETSC_DIR/SLEPC_DIR`。用户自装 PyTorch/CUDA/旧动态库 ⊥ 覆盖 packaged Python/CUDA/Nix；DISPLAY/Wayland + host NVIDIA driver discovery 保留；污染环境 install + doctor real/complex64/complex128 ! `status=ok` | flake.nix; scripts/release/easy-install/*; tests/unit/test_easy_installer_hardening.py |
