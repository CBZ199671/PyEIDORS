# Deprecated dependency entrypoint.
#
# PyEIDORS uses the locked Nix + uv environment:
#
#   nix develop
#   uv sync
#
# See docs/NIX_FENICSX.md for the maintained setup. This file is intentionally
# empty so pip-based installs do not recreate an unsupported runtime.
