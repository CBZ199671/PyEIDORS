#
# Minimal Python dependencies for running PyEidors.
#
# Recommended workflow:
#   - Use the FEniCS Docker image listed in `README.md`
#   - Install this project with `pip install -e .` (see `pyproject.toml`)
#
# NOTE: FEniCS/DOLFIN is provided by the Docker image and is not listed here.
#

numpy~=1.26.4
scipy~=1.12.0
matplotlib~=3.5.0
pandas~=2.3.0
h5py~=3.11.0
pyyaml~=5.4.1

# Mesh tooling (needed when generating meshes via Gmsh/meshio)
meshio>=5.3.4
gmsh>=4.11.1


